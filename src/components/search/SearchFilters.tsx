import { categories } from '../../data/mockListings';
import { ListingFilters } from '../../types';

export function SearchFilters({ filters, onChange }: { filters: ListingFilters; onChange: (filters: ListingFilters) => void }) {
  const update = (key: keyof ListingFilters, value: string | number) => onChange({ ...filters, [key]: value });
  return <section className="card p-5"><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4"><label className="space-y-2"><span className="label">Textsuche</span><input className="input" value={filters.query} onChange={(e) => update('query', e.target.value)} placeholder="Brot, Kimchi, Anbieter..."/></label><label className="space-y-2"><span className="label">Kategorie</span><select className="input" value={filters.category} onChange={(e) => update('category', e.target.value)}><option value="">Alle Kategorien</option>{categories.map((category) => <option key={category}>{category}</option>)}</select></label><label className="space-y-2"><span className="label">Ort</span><input className="input" value={filters.city} onChange={(e) => update('city', e.target.value)} placeholder="z. B. Freiburg"/></label><label className="space-y-2"><span className="label">PLZ für Radius</span><input className="input" value={filters.zipCode} onChange={(e) => update('zipCode', e.target.value)} placeholder="79100"/></label><label className="space-y-2"><span className="label">Radius: {filters.radiusKm} km</span><input type="range" min="1" max="50" value={filters.radiusKm} onChange={(e) => update('radiusKm', Number(e.target.value))} className="w-full accent-sage"/></label><label className="space-y-2"><span className="label">Zutaten</span><input className="input" value={filters.ingredients} onChange={(e) => update('ingredients', e.target.value)} placeholder="Roggen, Chili..."/></label><label className="space-y-2"><span className="label">Allergene</span><input className="input" value={filters.allergens} onChange={(e) => update('allergens', e.target.value)} placeholder="Gluten, Sesam..."/></label><label className="space-y-2"><span className="label">Sortierung</span><select className="input" value={filters.sort} onChange={(e) => update('sort', e.target.value)}><option value="new">Neue Anzeigen</option><option value="popular">Beliebte Anzeigen</option></select></label></div></section>;
}
